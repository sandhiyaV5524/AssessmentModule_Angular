import { Projectcompletion } from './projectcompletion';

describe('Projectcompletion', () => {
  it('should create an instance', () => {
    expect(new Projectcompletion("","","","","")).toBeTruthy();
  });
});
