import { Assignmentcompletion } from './assignmentcompletion';

describe('Assignmentcompletion', () => {
  it('should create an instance', () => {
    expect(new Assignmentcompletion(0,"","",false)).toBeTruthy();
  });
});
