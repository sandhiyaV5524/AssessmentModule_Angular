import { AuthBasicAuthInterceptor } from './auth-basic-auth-interceptor';

describe('AuthBasicAuthInterceptor', () => {
  it('should create an instance', () => {
    expect(new AuthBasicAuthInterceptor()).toBeTruthy();
  });
});
